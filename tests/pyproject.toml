[project]
name = "tooling"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sumedh Girish", email = "sumedh.girish@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "ectf>=2026.0.10",
    "pytest>=8.0.0",
]

[project.scripts]
scenario = "tooling.generate_groups:generate"
tests = "tooling.run_tests:run"

[build-system]
requires = ["uv_build>=0.10.8,<0.11.0"]
build-backend = "uv_build"
